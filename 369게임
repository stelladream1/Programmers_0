def solution(order):
    order = list(str(order))
    count = 0
    for i in range(len(order)):
        if order[i] == "3" or order[i] == "6" or order[i] == "9":
            count = count + 1 
        else:
            continue
    return count 
