def solution(sides):
    count  = 0
    count2 = 0
    for i in range(max(sides)-min(sides)+1,max(sides)+1): # 가장 큰 수가 주어졌을 때 
        count = count + 1
    for i in range(max(sides)+1, sum(sides)):
        count2 = count2 + 1
    return (count+count2)
